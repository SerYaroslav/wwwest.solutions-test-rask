const initialState = {
  new: null,
}

const reducer = (state = initialState, action) => {
  switch (action.type) {
    case 'test':
      return  
    default:
      return
  }
}

export default reducer;